/** Source mark (square PNG with circular mark on black). */
export const BRAND_LOGO_SOURCE = "/AiRAT_LOGO.PNG" as const;

/** Circular crop used in UI and favicons (no square black corners). */
export const BRAND_LOGO_ROUND = "/brand-logo-round.png" as const;

export const BRAND_NAME = "AiRAT" as const;
