type Props = {
  slug: string;
  size?: number;
  className?: string;
  monochrome?: boolean;
  colorHex?: string;
};

/** Icons disabled until image assets are added back. */
export default function SimpleIconImg(_props: Props) {
  return null;
}
